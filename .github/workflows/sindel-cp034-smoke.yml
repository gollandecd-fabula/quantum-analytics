name: SINDEL CP034 Actions smoke
on:
  push:
    branches: [sindel-cp034-buildfactory]
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo SINDEL_CP034_ACTIONS_OK
          uname -a
          free -h
          df -h
