name: Quantum Any File Extension Legacy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired workflow notice
        run: echo "Use any-file-extension-r3.yml"
