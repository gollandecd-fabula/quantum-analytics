# Bootstrap Import Instructions

1. Create a private GitHub repository named `quantum-analytics` under `gollandecd-fabula`.
2. Install or grant the ChatGPT GitHub connector access to that repository.
3. Import the contents of this package at the repository root.
4. Protect `main`.
5. Require Pull Requests and status checks.
6. Do not activate Actions until every third-party action is pinned to an immutable commit SHA.
7. Verify file hashes against `docs/evidence/ARTIFACT_MANIFEST.json`.
8. Update `CURRENT_STATE.md` with the repository URL and initial commit SHA.
